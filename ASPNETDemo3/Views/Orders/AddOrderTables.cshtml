@model ASPNETDemo3.Data.Order

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var foods = ViewBag.Foods;
}

<h4>Thông tin đặt tiệc</h4>
<p>@Model.customerName</p>
<div class="card">
    <div class="card-body">
        <div class="row">
            <div class="col-6">
                <h4>Thêm mới thực đơn</h4>
            </div>
            <div class="col-6 text-end">
                <a class="btn btn-info" asp-controller="" asp-action="Index">Thêm</a>
            </div>  
        </div>
        <form asp-controller="Orders" asp-action="AddOrderTables">
            @* <input asp-for="OrderId" hidden value=@Model.Id/>
            <div class="form-group">
                <label asp-for="table_name" class="control-label"></label>
                <input asp-for="table_name" class="form-control" />
                <span asp-validation-for="table_name" class="text-danger"></span>
            </div> *@
        </form>
    </div>
</div>